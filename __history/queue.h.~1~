//---------------------------------------------------------------------------

#ifndef queueH
#define queueH
//---------------------------------------------------------------------------
#endif

#include "list1.h"

class queue : public list
{
  public:
	list *frnt, *rear;
	void initiz(queue *q);
	int isempty(queue *q);
	void insert(queue *q, int x, int check);
	int test(queue *q);
};